import { Box, Typography } from '@mui/material'

const Home = () => {
    
    return (
        <Box sx={{ display: 'flex', justifyContent: 'center', padding: '20px' }}>
            <Typography variant='h4' sx={{ fontWeight: 600 }}>WELCOME HOME</Typography>
        </Box>
    )
}

export default Home
